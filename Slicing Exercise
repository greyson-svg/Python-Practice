#From the string "Welcome to Python 101: Strings", extract text and create/
#print a new string that says
#* "1 Welcome Ring To Tyler"
#* Every first letter in a word should be capitalized(title format)


msg = 'welcome to Python 101: Strings'

print(msg)

msg2 = msg[-10] + ' ' + msg[:7] + ' ' + msg[-5:-1] + ' ' + msg[8:10] + ' ' + msg[-6] + msg[12] + msg[2] + msg[1] + msg[-5]

print(msg2.title())


#Print the same string backwards ...

print(msg2[::-1])
